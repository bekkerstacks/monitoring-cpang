version: '3.5'

services:
  mysql:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    networks:
      - private
    environment:
      - MYSQL_ROOT_PASSWORD=password

networks:
  private:
    external: true
